/**
 * Public API for the `settings` feature.
 *
 * This file is the only importable surface of the feature. Anything not
 * re-exported here is private to `src/features/settings/` and importing it from
 * outside fails lint (criterion B1).
 */
export {};
