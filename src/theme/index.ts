/**
 * Design tokens. Three layers — primitive, semantic, component — arriving in
 * tasks 3 to 5. This is the only place in the repository allowed to contain
 * literal colour values (criterion C2).
 */
export {};
