/**
 * `src/app/**` holds routes and layouts. A file here that is neither must be
 * prefixed with an underscore so expo-router ignores it, which is what the
 * underscore on this filename demonstrates (criterion B3).
 */
export const ROOT_ROUTE = '/' as const;
