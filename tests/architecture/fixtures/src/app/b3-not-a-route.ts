// B3 — lives in the routes directory, has no default export, and is not
// underscore-prefixed. It is a helper pretending to be a route.
export const formatAmount = (cents: number) => String(cents);
