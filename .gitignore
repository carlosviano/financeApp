# Secrets and local machine config
.env
.env.local
.mcp.json

# Dependencies and build output
node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo

# Native projects — generated by prebuild (CNG), never edited by hand
/ios
/android

# macOS
.DS_Store

# Design canvas — reference material, not a build input. See design/README.md.
# The palette extraction (canvas-tokens.json) IS committed so that acceptance
# criterion C9 can run in CI without the canvas being present.
design/*
!design/README.md
!design/canvas-tokens.json
!design/extract-tokens.mjs

# Learning notes — personal study material, not project documentation.
# See notes/README.md (also ignored) for what lives here and why.
notes/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli